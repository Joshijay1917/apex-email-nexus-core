services:
  postgres:
    image: postgres:17
    container_name: emailnexus_db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: emailnexus
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: